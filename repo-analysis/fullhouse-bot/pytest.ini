[pytest]
markers =
    integration: end-to-end match and packaging smoke tests
    training: training and parity checks for the Deep CFR pipeline
    slow: heavier parity checks that are still part of local validation
filterwarnings =
    ignore:.*:pyparsing.warnings.PyparsingDeprecationWarning
